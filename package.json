{
  "name": "component-name-grabber",
  "version": "1.0.0",
  "description": "A Figma plugin that scans for components and displays their information",
  "main": "main.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.82.0",
    "typescript": "^5.0.0"
  }
} 